## 1. 回归测试

- [x] 1.1 为下一次锁模、START 和待开始解锁补充旧版待开始 JSON 的回归测试，并保留不可信数据拒绝用例

## 2. 最小兼容修复

- [x] 2.1 仅规范化旧版待开始子作业的空状态，并在直接解锁前复用既有懒持久化路径

## 3. 验证

- [x] 3.1 运行相关 Service（服务）测试、模块构建和 OpenSpec 严格校验，确认无需数据库迁移或 QT App 改动
- [x] 3.2 保留主规格既有 ID 为空场景，并以独立场景补充旧版空子状态兼容语义
